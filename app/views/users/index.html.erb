<% provide(:title, 'All users') %>
<h1>All users</h1>


<%= will_paginate %>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
<ul class="users">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>
  </tbody>
</table>
<%= will_paginate %>

<p>New employee? <%= link_to "Sign up now!", signup_path %></p>