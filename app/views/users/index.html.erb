<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= link_to "Download Excel", users_path(format: "xls"), :class=>"btn btn-primary"%>
<%= will_paginate @users %>
<table class="table table-striped">
  <thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
  </tr>
  </thead>

  <tbody>
<ul class="pagination">
  <% @users.each do |user| %>
      <%= render user %>
  <% end %>
</ul>
  </tbody>
</table>
<%= will_paginate @users %>

<p>New employee? <%= link_to "Sign up now!", signup_path %></p>